export type Product = {
  // id?: number;
  code: string;
  name: string;
  price: number;
  description: string;
  category: string;
  brand: string;
  model: string;
  stock: number;
  // [key: string]: string | number | boolean | undefined;
};
